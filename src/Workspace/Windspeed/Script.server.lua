local value = script.Parent.Value.X
local wholevalue = script.Parent.Value
while wait(math.random(30,60)) do 
	--wait(math.random(30,60))
	local number = math.random(-50,50)
	--wait(0.01)
	value = number
	--print("Wind changed to "..value)
end